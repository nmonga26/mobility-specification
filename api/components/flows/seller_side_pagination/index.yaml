summary: Unreserved Entry Pass - Seller App Pagination Flow (Culture-Heritage)
details: &details
  - &details_0
    description: The illustrative flow to perform a transaction to create an
      ecosystem where entry tickets/ passes can be bought and sold over the
      network.
    mermaid: >-
      sequenceDiagram
        title Unreserved Entry Pass Discovery
        Buyer Platform (BAP)->>Gateway (BG): search
        Gateway (BG) ->> Buyer Platform (BAP): ACK
        Gateway (BG)->>Registry: Lookup Seller Platforms (lookup)
        Registry->>Gateway (BG): List of Seller Platforms (200 OK)
        Gateway (BG)->>Seller Platform (BPP): search
        Seller Platform (BPP)->>Gateway (BG) : ACK
        Seller Platform (BPP)->>Buyer Platform (BAP): Publish Catalog of Seller 1 (on_search)
        Buyer Platform (BAP)->>Seller Platform (BPP): ACK 

steps: &steps
  - &steps_0
    summary: Search for the Unreserved Entry Pass
    api: search
    details: &steps_0_details
      - &steps_0_details_0
        description: Users searches over the network to avail the Unreserved Entry Pass
        mermaid: >-
          sequenceDiagram
            rect rgb(191, 223, 255)
            Buyer Platform (BAP)->>Gateway (BG): search
            Gateway (BG) ->> Buyer Platform (BAP): ACK
            Gateway (BG)->>Registry: Lookup Seller Platforms (lookup)
            Registry->>Gateway (BG): List of Seller Platforms (200 OK)
            Gateway (BG)->>Seller Platform (BPP): search
            Seller Platform (BPP)->>Gateway (BG) : ACK
            end
            Seller Platform (BPP)->>Buyer Platform (BAP): Publish Catalog of Seller 1 (on_search)
            Buyer Platform (BAP)->>Seller Platform (BPP): ACK  
    reference: if any
    example: &steps_0_example
      summary: Search for unreserved entry pass
      value: &steps_0_example_value
        $ref: ../../examples/unreserved-entry-pass/search/Search_for_unreserved_entry_pass_2.yaml
  - &steps_1
    summary: Return a catalog of all unreserved-entry-pass services
    api: on_search
    details: &steps_1_details
      - &steps_1_details_0
        description: Provider platform sends the catalog of all the services
        mermaid: >-
          sequenceDiagram
            Buyer Platform (BAP)->>Gateway (BG): search
            Gateway (BG) ->> Buyer Platform (BAP): ACK
            Gateway (BG)->>Registry: Lookup Seller Platforms (lookup)
            Registry->>Gateway (BG): List of Seller Platforms (200 OK)
            Gateway (BG)->>Seller Platform (BPP): search
            Seller Platform (BPP)->>Gateway (BG) : ACK
            rect rgb(191, 223, 255)
            Seller Platform (BPP)->>Buyer Platform (BAP): Publish Catalog of Seller 1 (on_search)
            Buyer Platform (BAP)->>Seller Platform (BPP): ACK 
            end 
    reference: if any
    example: &steps_1_example
      summary: Return a catalog for unreserved entry pass
      value: &steps_1_example_value
        $ref: ../../examples/unreserved-entry-pass/on_search/on_search_request_1.yaml
  - &steps_1
    summary: Return a catalog of all unreserved-entry-pass services
    api: on_search
    details: &steps_1_details
      - &steps_1_details_0
        description: Provider platform sends the catalog of all the services
        mermaid: >-
          sequenceDiagram
            participant Buyer Platform (BAP)
            participant Seller Platform (BPP)
            rect rgb(191, 223, 255)
            Buyer Platform (BAP)->>Seller Platform (BPP): search
            Seller Platform (BPP)-->>Buyer Platform (BAP):ACK
            end
            Seller Platform (BPP)->>Buyer Platform (BAP):on_search
            Buyer Platform (BAP)-->>Seller Platform (BPP): ACK 
    reference: if any
    example: &steps_1_example
      summary: Return a catalog for unreserved entry pass
      value: &steps_1_example_value
        $ref: ../../examples/unreserved-entry-pass/on_search/Return_a_catalog_for_unreserved_entry_pass_2.yaml
  - &steps_1
    summary: Return a catalog of all unreserved-entry-pass services
    api: on_search
    details: &steps_1_details
      - &steps_1_details_0
        description: Provider platform sends the catalog of all the services
        mermaid: >-
          sequenceDiagram
            participant Buyer Platform (BAP)
            participant Seller Platform (BPP)
            Buyer Platform (BAP)->>Seller Platform (BPP): search
            Seller Platform (BPP)-->>Buyer Platform (BAP):ACK
            rect rgb(191, 223, 255)
            Seller Platform (BPP)->>Buyer Platform (BAP):on_search
            Buyer Platform (BAP)-->>Seller Platform (BPP): ACK
            end 
    reference: if any
    example: &steps_1_example
      summary: Return a catalog for unreserved entry pass
      value: &steps_1_example_value
        $ref: ../../examples/unreserved-entry-pass/on_search/on_search_request_3.yaml
